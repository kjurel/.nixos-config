return {
  git = {
    enable = true,
  },

  renderer = {
    highlight_git = true,
    icons = {
      -- glyphs = require("nvim-nonicons.extentions.nvim-tree").glyphs,
      show = {
        git = true,
      },
    },
  },
}

